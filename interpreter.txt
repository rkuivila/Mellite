val tl = doc.collectObjects {
  case proc.Timeline.Obj(tl) => tl.elem.peer
} .head

val Seq(a, b) = Seq("A", "B").map { n => 
  tl.iterator.flatMap(_._2).toList.find(_.value.attr.name == n).get
}

val as = a.span
val bs = b.span.asVar

bs() = as shift 4.secframes
